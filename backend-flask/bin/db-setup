#! /usr/bin/bash

CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL="db-setup"
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"

BIN_PATH="$(realpath .)/bin"

# Drop the db
bash "$BIN_PATH/db-drop $1"
# create the db
psql $CON_URL < $SCHEMA_PATH
# Seed tables
psql $CON_URL < $SEED_PATH